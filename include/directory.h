#ifndef DIRECTORY
#define DIRECTORY

// reads files in a directory, returns vectors of filenames

#include <string>
#include <vector>

class Directory {
  private:

  public:
    std::vector <std::string> getPics(std::string);
};

#endif
